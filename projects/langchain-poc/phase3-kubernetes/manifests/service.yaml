apiVersion: v1
kind: Service
metadata:
  name: k8s-agent
  namespace: langchain-poc
spec:
  type: NodePort
  selector:
    app: k8s-agent
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
